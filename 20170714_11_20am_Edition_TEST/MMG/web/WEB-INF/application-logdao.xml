<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns:beans="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:p="http://www.springframework.org/schema/p"
       xmlns:task="http://www.springframework.org/schema/task"
       xmlns:aop="http://www.springframework.org/schema/aop"
       xmlns:tx="http://www.springframework.org/schema/tx"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
       http://www.springframework.org/schema/task http://www.springframework.org/schema/task/spring-task-3.0.xsd
       http://www.springframework.org/schema/aop http://www.springframework.org/schema/aop/spring-aop-3.0.xsd
       http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx-3.0.xsd
       http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-3.0.xsd">

	<!-- ========================= RESOURCE DEFINITIONS ========================= -->

<!-- Creating the Logging Data SOurce for Logging Connection Pooling -->
    <beans:bean id="logDataSource" class="com.mchange.v2.c3p0.ComboPooledDataSource"
        destroy-method="close">
        <beans:property name="driverClass">
            <beans:value>${MMG.LOGGING.DATABASE.DIALECT}</beans:value>
        </beans:property>
        <beans:property name="jdbcUrl">
            <beans:value>${MMG.LOGGING.DATABASE.URL}</beans:value>
        </beans:property>

        <beans:property name="properties">
            <beans:props>
                <beans:prop key="user">${MMG.LOGGING.DATABASE.USERNAME}</beans:prop>
                <beans:prop key="password">${MMG.LOGGING.DATABASE.PASSWORD}</beans:prop>
            </beans:props>
        </beans:property>

         <beans:property name="idleConnectionTestPeriod">
            <beans:value>${MMG.LOGGING.DATABASE.POOLING.C3PO.IDLETESTPERIOD}</beans:value>
        </beans:property>
        <beans:property name="maxIdleTime">
            <beans:value>${MMG.LOGGING.DATABASE.POOLING.C3PO.TIMEOUT}</beans:value>
        </beans:property>

    </beans:bean>

<!-- Creating the Logging Session Factory for Connection Pooling -->
    <beans:bean id="logSessionFactory"
		class="org.springframework.orm.hibernate3.annotation.AnnotationSessionFactoryBean">
        <beans:property name="dataSource">
            <beans:ref local="logDataSource" />
        </beans:property>
        <!-- Configuring the Hibernate Connection Properties -->
        <beans:property name="hibernateProperties">
            <beans:props>
                <beans:prop key="hibernate.dialect">${MMG.LOGGING.DATABASE.DIALECT}</beans:prop>
                <beans:prop key="hibernate.show_sql">${MMG.LOGGING.DATABASE.HIBERNATE.SHOWSQL}</beans:prop>
                <beans:prop key="hibernate.c3p0.min_size">${MMG.LOGGING.DATABASE.POOLING.C3PO.MINSIZE}</beans:prop>
                <beans:prop key="hibernate.c3p0.max_size">${MMG.LOGGING.DATABASE.POOLING.C3PO.MAXSIZE}</beans:prop>
                <beans:prop key="hibernate.c3p0.idle_test_period">${MMG.LOGGING.DATABASE.POOLING.C3PO.IDLETESTPERIOD}</beans:prop>
                <beans:prop key="hibernate.c3p0.timeout">${MMG.LOGGING.DATABASE.POOLING.C3PO.TIMEOUT}</beans:prop>
                <beans:prop key="hibernate.c3p0.max_statements">${MMG.LOGGING.DATABASE.POOLING.C3PO.MAXSTATEMENTS}</beans:prop>
                <!--<beans:prop key="hibernate.cache.use_query_cache">${MMG.LOGGING.DATABASE.POOLING.C3PO.USEQUERYCACHE}</beans:prop>-->
                <beans:prop key="hibernate.c3p0.acquire_increment">${MMG.LOGGING.DATABASE.POOLING.C3PO.INCREMENT}</beans:prop>
                <beans:prop key="hibernate.c3p0.validate">true</beans:prop>   <!-- THis is an Important Part -->
                <beans:prop key="hibernate.c3p0.preferredTestQuery">select 1;</beans:prop>
                
            </beans:props>
        </beans:property>

        <beans:property name="annotatedClasses">
            <beans:list>
                <!--<value>com.interrait.dell.contentmanager.db.log.vo.DellCMLogDetails</value> -->
                <beans:value>com.mazdausa.mmg.db.vo.AccessLogVO</beans:value>

            </beans:list>
        </beans:property>
    </beans:bean>

     <!-- Configure the Logging Transaction manager for a single Hibernate Logging SessionFactory(alternative to JTA) -->
    <beans:bean id="logTransactionManager"
		class="org.springframework.orm.hibernate3.HibernateTransactionManager">
        <beans:property name="sessionFactory">
            <beans:ref local="logSessionFactory" />
        </beans:property>
    </beans:bean>

   <!-- Configure the Logging Hibernate Template -->
    <beans:bean id="logHibernateTemplate"
            class="org.springframework.orm.hibernate3.HibernateTemplate">
        <beans:property name="sessionFactory">
            <beans:ref local="logSessionFactory" />
        </beans:property>
    </beans:bean>

      <!-- Configuringthe Open Session In View Interceptor for the Logging Hibernate Session -->
    <beans:bean id="logOpenSessionInViewInterceptor" class="org.springframework.orm.hibernate3.support.OpenSessionInViewInterceptor">
        <beans:property name="sessionFactory"  ref="logSessionFactory"/>

            <!--<property name="singleSession" value="true"/> -->
        <beans:property name="flushModeName">
            <beans:value>FLUSH_ALWAYS</beans:value>
        </beans:property>
    </beans:bean>

  

</beans:beans>